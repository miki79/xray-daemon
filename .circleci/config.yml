version: 2
jobs:
  build:
    docker:
      - image: docker

    steps:
      - checkout
      - run:
          name: configure NPM registry
          command: wget https://s3.dualstack.${REGION}.amazonaws.com/aws-xray-assets.${REGION}/xray-daemon/aws-xray-daemon-linux-2.x.zip
      - run:
          command: wget https://s3.dualstack.${REGION}.amazonaws.com/aws-xray-assets.${REGION}/xray-daemon/aws-xray.gpg
      - run:
          command: gpg --import aws-xray.gpg        
      - run:
          command: gpg --verify aws-xray-daemon-linux-2.0.0.zip.sig aws-xray-daemon-linux-2.0.0.zip      
            